# FUTTO

Web app FUTTO (« Le foot nous unit ») — Côte d’Ivoire, FCFA, notes en étoiles.

Ce dépôt est **indépendant** de `futtodemo`. Ce n’est pas le même projet.

## Lancer en local

```bash
npm install
npm run dev
```

Ouvre [http://localhost:3000](http://localhost:3000).
